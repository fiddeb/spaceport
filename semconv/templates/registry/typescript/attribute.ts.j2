{%- macro ts_const(text) -%}
{{ text | replace('.', '_') | upper }}
{%- endmacro -%}
/**
 * Semantic convention attribute constants.
 *
 * DO NOT MODIFY: This code is autogenerated.
 * See templates/registry/typescript/attribute.ts.j2.
 */

// --- Attribute keys ---
{% for attr in ctx.attributes | attribute_sort %}
{{ attr.brief | comment }}
export const {{ ts_const(attr.name) }} = "{{ attr.name }}";
{%- endfor %}

// --- Enum values ---
{% for attr in ctx.attributes | attribute_sort %}
{%- if attr.type.members is defined %}

// Enum values for {{ attr.name }}.
{%- for member in attr.type.members %}
export const {{ ts_const(attr.name) }}_{{ member.id | screaming_snake_case }} = "{{ member.value }}";
{%- endfor %}
{%- endif %}
{%- endfor %}
